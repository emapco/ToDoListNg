import { FormatTextDisplayPipe } from './format-description-display.pipe';

describe('FormatDescriptionDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTextDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
