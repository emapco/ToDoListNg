import { FormatDateDisplayPipe } from './format-date-display.pipe';

describe('FormatDateDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
