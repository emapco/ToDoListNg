import { FormatTimeDisplayPipe } from './format-time-display.pipe';

describe('FormatTimeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTimeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
